services:
  gitbackup:
    container_name: gitbackup
    hostname: gitbackup
    image: gitbackup
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: "no"
    volumes:
      - /tmp:/tmp
      - ./config.json5:/app/config.json5